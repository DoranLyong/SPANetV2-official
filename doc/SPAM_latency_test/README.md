```bash
pip install uv
uv sync --index-strategy unsafe-best-match --upgrade
```

```bash
#-- Run the uv env. for Linux/Mac
source .venv/bin/activate 

#-- Run the uv env. for Window
.venv\Scripts\activate
```